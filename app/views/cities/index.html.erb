<% if params[:param_as_id] %>
    <%= "<option value>Выберите город</option>".html_safe + options_from_collection_for_select(collection, :id, :name).html_safe %>
<% else %>
    <%= "<option value>Выберите город</option>".html_safe + options_from_collection_for_select(collection, :slug, :name).html_safe %>
<% end %>
